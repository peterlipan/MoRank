# environment
visible_gpus: '0'
seed: 42

# dataset
dataset: 'metabric'
task: 'survival'
feature_file: './datasets/METABRIC/cleaned_features_final.csv'
label_file: './datasets/METABRIC/label.csv'
checkpoints: './checkpoints'
results: './results'

kfold: 5
stratify: True
n_bins: 25
normalize: False

# model
method: 'deepsurv' # 'deephit', 'deepsurv', 'discrete'
backbone: 'MLP'
n_layers: 2
d_hid: 1024
dropout: 0.
activation: 'tanh'

# ours
queue_size: 512
lambda_rank: 0.
ema_decay: 0.999
queue: False
virt_ratio: 1.0
alpha: 0.4

# training
workers: 0
optimizer: 'Adam'
epochs: 100
batch_size: 256
lr: 2.0e-4
lr_policy: 'cosine'
weight_decay: 0.01
verbose: True
